<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ tagName }}</title>
</head>
<body>
<ul>
<h1>{{ tagName }}</h1>
{% for post in posts -%}
	{%- for postTag in post.tags -%}
		{%- if postTag === tagName -%}
			<li><a href="{{ post._path.slice(6) }}">{{ post.title }}</a></li>
		{%- endif -%}
	{%- endfor -%}
{%- endfor %}
</ul>
</body>
</html>